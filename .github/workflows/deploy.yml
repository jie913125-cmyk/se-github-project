name: CI/CD Pipeline
# 触发机制：当代码 Push 到 main 分支时自动运行
on:
  push:
    branches: [ main ]

jobs:
  # 任务 1：构建并推送 Docker 镜像到 Docker Hub
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码（Checkout）
        uses: actions/checkout@v4
      
      - name: 🐳 登录 Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: 🔨 构建并推送镜像
        uses: docker/build-push-action@v5
        with:
          context: .  # 构建上下文为当前目录
          tags: ${{ secrets.DOCKER_USERNAME }}/project-alpha:latest  # 镜像标签（仓库名:版本）
          push: true  # 启用镜像推送功能

  # 任务 2：部署镜像到云服务器（依赖任务 1 成功执行）
  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 SSH 远程部署
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST_IP }}  # 云服务器 IP
          username: ubuntu  # 服务器登录用户名（默认 ubuntu）
          key: ${{ secrets.SSH_PRIVATE_KEY }}  # SSH 私钥
          script_stop: true  # 脚本执行出错时立即停止
          # 远程执行的部署脚本
          script: |
            echo "--- 开始部署流程 ---"
            # 1. 停止并删除旧容器（若存在，不存在则忽略报错）
            docker stop my-app || true
            docker rm my-app || true
            # 2. 拉取 Docker Hub 最新镜像
            echo "正在拉取最新镜像..."
            docker pull ${{ secrets.DOCKER_USERNAME }}/project-alpha:latest
            # 3. 启动新容器（--restart always 确保服务器重启后容器自动启动）
            echo "启动新容器..."
            docker run -d \
              --name my-app \
              --restart always \
              -p 8080:8080 \
              ${{ secrets.DOCKER_USERNAME }}/project-alpha:latest
            echo "--- 部署完成 ---"
